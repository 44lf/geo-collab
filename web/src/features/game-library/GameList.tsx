import type { GameListItem } from "../../types";
type Props = { games: GameListItem[]; selectedId: number | null; onSelect: (id: number) => void; loaded: boolean };
export function GameList(_props: Props) { return null; }
