import type { SortKey } from "./GameLibraryWorkspace";
type Props = { total: number; q: string; onQ: (v: string) => void; sort: SortKey; onSort: (s: SortKey) => void };
export function GameLibraryHeader(_props: Props) { return null; }
