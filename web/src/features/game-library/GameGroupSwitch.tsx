import type { Seg } from "./GameLibraryWorkspace";
type Props = { seg: Seg; onSeg: (s: Seg) => void; mainCount: number; companionCount: number };
export function GameGroupSwitch(_props: Props) { return null; }
