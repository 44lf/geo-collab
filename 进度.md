# Geo 协作发布平台进度

更新时间：2026-05-11

## 总体判断

当前整体进度略快于原排期。5月7日-5月11日的本地 MVP 与基础工程能力已经基本完成；5月11日-5月15日的云端浏览器发布能力已经完成关键 Spike，正在从验证阶段进入正式后端化阶段。

## 已完成

### 本地 MVP 与基础工程能力

- FastAPI、React、SQLite/Alembic、本地数据目录和启动器已跑通。
- 文章、资源、账号、任务、发布记录、日志等核心模块已有基础实现。
- 本地 exe MVP 已完成过构建验证。
- Alembic 迁移链路可用。
- 头条发布器已接入任务执行主链路。

### 头条号发布 Spike

- 已验证头条号登录态保存和复用。
- 已跑通发布页进入、标题填充、正文填充、封面上传、正文图片上传。
- 已验证发布预览链路。
- 已完成一次头条真实发布成功验证。
- 已处理部分营销/引导弹窗的脚本关闭逻辑。

### 云服务器验证

- 阿里云 Ubuntu 22.04.5 环境已完成基础验证。
- Chromium、Xvfb、x11vnc、websockify/noVNC 已能运行。
- 本地浏览器可通过 SSH 隧道访问 noVNC。
- 云端扫码登录和发布页操作已验证。
- 云端安全发布 Smoke 已跑通，能进入发布预览并等待确认。
- 云端真实发布链路已经验证成功。

### 后端能力补强

- 发布浏览器 channel、executable path、并发数已配置化。
- 发布并发上限已按 5 个记录控制。
- 已加入 `waiting_user_input` 状态，用于扫码、验证码、登录失效等人工介入场景。
- 已加入人工介入恢复接口，处理完成后可重新入队。
- 任务状态机已覆盖人工确认、人工介入、失败、恢复等场景。
- Python 3.10 兼容问题已修复。

## 测试情况

- 头条发布器相关测试通过。
- 任务状态机测试通过。
- 删除保护、模型、账号、文章、资源、发布校验等受影响测试通过。
- FTS 和迁移测试通过。
- 完整 `server/tests` 曾受旧 launcher 测试耗时影响超时，非本轮改动直接失败点。

## 当前缺口

- noVNC/Xvfb/Chromium 仍偏手工启动，还没有正式纳入后端会话管理。
- 前端还缺少人工介入入口，用户还不能从后台直接打开对应任务浏览器。
- 云端账号鉴权还没做，当前仍偏本地单用户模式。
- 数据库还没有从 SQLite 切换到 MySQL。
- 飞书通知还没有接入真实 API。
- 管理后台的任务、日志、截图、人工介入体验还需要补齐。
- 压测和长时间稳定性验证还没开始。

## 按排期对照

### 5月7日-5月11日：本地 MVP 与基础工程能力

状态：基本完成。

剩余主要是整理文档、补少量测试和修掉历史 launcher 测试超时问题，不影响进入下一阶段。

### 5月11日-5月15日：云端浏览器发布能力

状态：已完成约 60%-70%。

已经完成云端环境、远程浏览器、扫码登录、登录态复用、发布页操作和真实发布验证。接下来要把这些能力产品化，重点是浏览器会话生命周期、noVNC 入口、任务状态联动和自动清理。

### 5月16日-5月22日：账号权限与数据持久化

状态：未正式开始。

需要在云端发布能力收敛后开始做用户登录、账号鉴权、接口权限、会话管理、MySQL 迁移和备份回滚。

### 5月23日-5月30日：任务调度、人工介入与恢复机制

状态：部分提前开始。

并发控制、同账号互斥基础、人工介入状态、恢复接口已经提前做了一部分。后续还要补前端入口、浏览器清理、任务超时、失败截图、重启恢复和更完整的异常分类。

### 5月31日-6月7日：管理后台、通知与稳定性收尾

状态：未正式开始。

需要等云端发布、鉴权、MySQL、任务恢复机制稳定后集中做后台体验、飞书通知、压测修复和上线准备。

## 下一步

1. 实现后端浏览器会话管理，托管 Xvfb、x11vnc、websockify 和 Chromium。
2. 给 `waiting_user_input` 增加前端入口，能从发布记录直接打开 noVNC。
3. 把当前云端 Spike 命令收敛成可配置服务启动方式。
4. 补任务失败截图、关键日志和浏览器清理。
5. 开始设计云端账号鉴权和 MySQL 迁移方案。
