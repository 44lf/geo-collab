# DEV checkpoint — 2026-07-29

## Scope and status

This checkpoint exercised the Phase 1 runtime from the local Windows host against the DEV
MySQL/MinIO host at `172.25.20.57`. It deliberately used the isolated
`geo_collector_dev_test` database because the shared `geo_dev` database reports Alembic revision
`0067_game_cull_and_manual`, which is not present in this repository and cannot be passed directly
to the current Alembic chain.

Result: **isolated DEV E2E passed; task 10.2 remains open until the shared `geo_dev` migration
history is reconciled and the same evidence is repeated there.**

## Shared geo_dev reconciliation dry-run

`server.scripts.reconcile_legacy_game_migration` now provides an incident-specific, guarded
reconciliation path. It verifies the historical game-library tables, columns, indexes, and
foreign keys; repairs the missing current 0065/0066 effects; moves the equivalent historical
revision to current 0069; and pins the normal Alembic upgrade target to committed revision 0073.
It requires both `--apply` and `--backup-confirmed`, uses a bounded metadata-lock timeout, and
performs a final schema/revision validation.

The isolated MySQL regression test simulated the historical branch, proved dry-run made no
changes, applied the bridge through 0073, and proved a repeat run was a no-op (`2 passed`).
The read-only command against shared `geo_dev` passed and planned exactly:

1. create `xhs_render_jobs` and its indexes (current 0065);
2. add and index `articles.content_type` (current 0066);
3. move `0067_game_cull_and_manual` to equivalent current `0069_game_cull_and_manual`;
4. run the normal migration chain through `0073_collector_gateway_model`.

No shared DEV DDL or version update was applied during this checkpoint. A restorable backup is
still required before the explicit apply command.

## Runtime

- Gateway: local HTTPS `127.0.0.1:8443`, external game-ingest execution mode, legacy schedulers
  and startup recovery disabled.
- Consumer: standalone local process, two-item batch, two-second poll, 120-second lease.
- Collector: local Windows process with public JSON configuration and the credential in Windows
  Credential Manager.
- Storage: private `geo-collector-inbox` plus the existing GEO business image bucket behavior.
- Database: fully migrated isolated `geo_collector_dev_test` on the DEV MySQL instance.

## E2E evidence

- Bounded refresh claim: `dev-e2e-refresh-20260729-02`.
- Job: `job-837b3909f9244a43a0a51544e703fbcd`.
- Run: `run-0fe726d4ceef247a53359c35` (`partial`: Baidu, Jiuyou, and TapTap succeeded;
  Yingyongbao returned a source error).
- Transfer: `transport-0fe726d4ceef247a53359c35`, `processed` on attempt 1.
- Receipt: 4/4 item receipts terminal; three imports anchored to GEO game `1`, one skipped source
  error.
- Archive: 613005 bytes, SHA-256
  `adc3472907cf97298fe3e0d05b99fcd7dc45e28d4c5f24f5836ee597768ec4d0`.
- Independent verification: Inbox size/hash matched MySQL, all ten Bundle inventory files matched
  their size/hash declarations, and the business MinIO cover bytes matched the Bundle media hash.
- Normalized game result: one GEO game with three unique source identities, merged score/platforms,
  and one deduplicated business image.
- Web management: authenticated Collector view showed backlog, both runs, the redacted timeline,
  processed transfer/archive identity, receipt 4/4, and all item outcomes.

## Restart/resume evidence

A scheduled catch-up Agent was stopped after all four source checkpoints and before packaging or
remote run registration. Resuming the exact claim `refresh-20260728T190000Z`:

- reused the same job/run identity;
- retained `request_count=4` and did not recrawl completed items;
- produced `transport-e1de66c13647ba3f87b027a0`;
- reached `processed` on attempt 1 with receipt 4/4;
- left the local job `completed`;
- preserved three unique game source identities and one business image.

The Agent was not left running because its default catch-up policy immediately schedules a missed
window. Task 10.4 must make that policy explicit before enabling the long-running DEV Agent.

## Defects found and fixed

1. Standard MinIO pre-signed PUT rejects an unsigned checksum metadata header. The Gateway now
   permits the standard PUT and recomputes SHA-256 from the private Inbox object when metadata is
   absent.
2. The standalone Consumer did not register all SQLAlchemy models, causing a dead letter while
   flushing `games.last_used_article_id`. A shared explicit ORM registry is now loaded at Consumer
   startup.
3. Heartbeats could overwrite the server-owned source allowlist. Heartbeats now preserve that
   allowlist and reject reported sources outside it.
4. CDN `Content-Type` could disagree with image bytes and fail the whole Bundle at the packaging
   boundary. The Collector now identifies supported images by signature and skips non-images.

The first dead-letter transfer
`transport-28984cd585ece0c573ad3c6c` remains intact as failure evidence.

## Verification gates

- Collector repository: Ruff format/check passed; `143 passed`.
- GEO Collector server tests: Ruff check passed; `149 passed`.
- Temporary browser-only HTTP listener was closed after visual verification.
- Production was not contacted, deployed, tagged, or enabled.

## Remaining blockers and risks

- Take and verify a restorable `geo_dev` backup, explicitly apply the tested reconciliation, and
  repeat the E2E against the shared database before task 10.2 can be checked.
- Run the remaining task 10.3 dependency/network/lease/partial-commit fault matrix.
- Decide and test the long-running Agent catch-up policy before task 10.4.
- `collector_nodes.last_success_at` was not populated by completed runs, so the node summary still
  shows no last-success time even though run/transfer evidence is correct.
- The business image bucket follows the existing GEO public-read policy; only the Inbox was
  verified private.
