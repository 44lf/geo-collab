# 2026-07-07 工作日志

## 续推 loop-skill-bundle 版本化（Claude Code 额度耗尽后被我接手）
- 背景：Claude Code 在 Task 5 收尾（生成 review package）时被 402 截停；Tasks 1-5 已提交（分支 `feat/loop-skill-bundle-versioning`）。
- 我并行派 sub-agent 完成 Tasks 6/7/8：
  - Task 6 后端：新建 `loop_skills/schemas.py`；`router.py` 3 出口端点改走 `versions_service` + 新增 5 管理端点（upload/enable/delete/list/download），含 ASCII 安全下载头、404/409 语义、启用版禁删（409）。
  - Task 7 MCP：`install_loop_skills` 加可选 `version` 参数透传，`MCP_TOOLS_COUNT` 保持 21。
  - Task 8 前端：`api/mcp.ts` 加 5 函数 + `BundleVersion` 类型；`McpConnectWorkspace` Section ⑤ 版本管理 UI（admin 门控）。
- Task 9 全量回归：后端 25 passed / ruff / mypy 全绿；前端 typecheck + build 全绿。
- 已提交 `41b11a8`（6 文件 +550/-42）。故意排除无关的 `claude-loops/generation-loop.md` 未提交改动。

## 用户交互备注
- 用户对"删除"高度敏感：看到 `DELETE` 端点 + commit 消息里的 "soft_delete" 一度以为在删代码。已核查确认工作区无任何文件删除（净 +527/-42，全为新增/改写），并解释软删除设计理由。后续涉及删除/破坏性改动需先明示。

## 合入 main
- 本地 `main` 与 `origin/main` 已分叉：origin/main 领先 25（video-loop、CI 并行测试等），本地 main 领先 3（/goal skill v12 + 两份飞书审核卡片文档）。
- 先 `git merge origin/main --no-edit`（无冲突）同步，再 `git merge feat/loop-skill-bundle-versioning --no-ff` → 合并提交 `ce13a12`。
- 结果：main 领先 origin/main 12 个提交、origin/main 0 独有（可干净 fast-forward push）。`claude-loops/generation-loop.md` 改动用 stash 暂存-恢复，保持未提交；`.workbuddy/` 未纳入。
- 未推远程（用户只说"合进main"，未要求 push）。

## 合并 worktree-feishu-review-card-h5 → main
- 该分支领先 main 18 提交（飞书审核卡片 + H5 免登），与 main 分叉（共祖 6f4ea57 在 main 历史里）。
- 5 处冲突：`connect_router.py`(MCP_TOOLS_COUNT 24→**25**)、`main.py`(挂 h5_public/h5_auth router)、`CLAUDE.md`(工具数 22→25)、`SKILL.md` v12(合并"重试锁定"+“逐篇发卡 notify_review_card”)、`version.py`(v12 bundle SHA)。
- **关键坑**：v12 SHA 不能只算 geo-goal-orchestrator/SKILL.md 单文件——`build_bundle()` 扫描**整个 templates/ 目录**。用 build_bundle_from_file_map 同款算法对合并后全文重算：LF=`a2cf5ffd…`、CRLF=`f33cabd3…`（与 disk 实算吻合）。已验证真实注册工具数=25（含 compose_video + notify_review_card）。
- 顺手修 feishu 分支写死的测试 `test_mcp_tools_count_is_22`→`is_25`(断言 22→25)。
- 回归 46 passed + ruff 全绿。提交 `85720eb`（--no-ff 合并提交）。claude-loops 改动用 stash 暂存-恢复，保持未提交；重复 stash 已 drop。
- main 现领先 origin/main 29 提交，可干净 fast-forward push（用户未要求推）。

## 清理本地分支/worktree + 同步 origin + 推送
- 用户指令："清理本地分支和worktree，切到main，确保本地main最新" → 随后"推上去"。
- `git fetch` 发现 origin/main 反超本地 4 提交（别人的视频 ruff/mypy 修复 + `test_mcp_connect.py` tools_count 21→24）：本地 main 领先 29、远程领先 4，**历史分叉**。
- 清理：`git worktree remove .claude/worktrees/feishu-review-card-h5`；`git branch -d feat/loop-skill-bundle-versioning worktree-feishu-review-card-h5`（均确认已合进 main）。仅剩 `main`，worktree list 干净。
- 同步：`git merge origin/main --no-ff` → 唯一冲突 `test_mcp_connect.py` 的 `tools_count`（HEAD 22 vs origin 24）。
- **冲突解决关键**：没用任一侧的值，而是运行时自省 `mcp._tool_manager._tools` 确认真实注册数 = **25**（catalog 9 + meta 4 + video 3 + action 9；`compose_video` 在 video.py、`notify_review_card` 在 action.py，各一次、无重复）。断言改为 `== 25`，与 `MCP_TOOLS_COUNT=25` 及 `test_mcp_status_count.py` 对齐。提交 `9f0c6ad`。
- 校验：从仓库根目录 `pytest --collect-only` 1243 测试零 import 错误（从 server/ 子目录跑会因顶层 `mcp` 被 `server/mcp` 影子化报错，需从根目录跑）。
- 推送：`git push origin main` → `9325c21..9f0c6ad main -> main`，fast-forward 成功，本地与 origin/main 完全同步（不再 ahead）。
- 依旧按约定排除：`claude-loops/generation-loop.md`(M)、`.workbuddy/`(??) 不进提交。
